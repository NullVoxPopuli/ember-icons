{{#let this.entry as |entry|}}
  {{#if (this.isComponent entry)}}

    <entry.component fill="current" ...attributes />

  {{else}}

    <svg ...attributes fill="current">
      <use xlink:href={{entry.href}}></use>
    </svg>

  {{/if}}
{{/let}}

{{!--
   <svg>
      <use xlink:href="/your-path-to-fontawesome/sprites/solid.svg#saxophone-fire"></use>
    </svg>
--}}
